package random;

import java.util.Random;

public class ComeRichiamareUnRandom {

    //per ottenere un numero random tra 0 e n-1 (n escluso):

    //Random.nextInt(n)
    // oppure
    //(int) (Math.random()*n)

    //la prima è più e produce risultati miglori della seconda

    //per ottenere un numero random tra 0 e n (con n incluso):

    //Random.nextInt(n+1)

    //per ottenere un numero random tra min e max(inclusi):

    //int val = Random.nextInt(max - min + 1) + min


}
